#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int _putchar(char c);
int is_integer(const char *str);
int get_coin_number(int amount, int nb_coins);

#endif
